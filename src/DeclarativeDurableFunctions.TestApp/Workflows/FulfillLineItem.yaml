workflow:
  name: Fulfill Line Item V2
  steps:
    - name: FulfillItem
      activity: FulfillLineItemActivity
      input: "{{input}}"
      output: result

    - name: NotifyWarehouse
      activity: SendConfirmationEmailActivity
      input: "Item {{input.data.lineItemId}} fulfilled"
      output: warehouseNotification
